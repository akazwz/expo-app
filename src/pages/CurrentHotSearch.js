import React from 'react';
import { View } from "react-native";
import { Text } from "react-native-elements";

const CurrentHotSearch = () => {
    return (
        <View>
            <Text h1>
                this is current hot search.
            </Text>
        </View>
    );
};

export default CurrentHotSearch;
